const divide = (a, b) => {
  return a / b;
};
const result = divide(2000, 100);

const square = (a) => {
  return a * a;
}
const result1 = square(100);

const add = (x, y) => {
  return x + y;
};
const result2 = add(2, 5);
